/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["i.redd.it","www.google.com","www.compgamer.com",'images.unsplash.com',"i0.wp.com","s.isanook.com"]
    }
};

export default nextConfig;

// ตรงนี้สามารถแยก comp แล้วอิมพอตค่าคอนฟิคมาได้เลย
